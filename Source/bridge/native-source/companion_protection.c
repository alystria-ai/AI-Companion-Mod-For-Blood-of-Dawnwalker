/* Independent source-tag protection bridge. It cannot dispatch population
 * operations and keeps its one prepared GameplayEffect explicitly rooted. */
#define COMPANION_PROTECTION
#include "companion_native.c"
